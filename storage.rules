rules_version = '2';

// Firebase Storage Security Rules
// Note: This portfolio project does not currently use Firebase Storage.
// All rules deny access by default.
service firebase.storage {
  match /b/{bucket}/o {
    // Deny all access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
