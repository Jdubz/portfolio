import ScrollButton from "../components/scroll-button"
import { GradientGlow } from "../../../components/GradientGlow"

export default () => (
  <>
    {/* Mask background icons behind text to prevent collision */}
    <div sx={{
      pointerEvents: 'none',
      position: 'absolute',
      inset: 0,
      maskImage: 'radial-gradient(220px 140px at 22% 44%, transparent 0, black 60%)',
      WebkitMaskImage: 'radial-gradient(220px 140px at 22% 44%, transparent 0, black 60%)',
    }} />

    <div sx={{
      mx: 'auto',
      maxWidth: 1120,
      display: 'grid',
      gridTemplateColumns: ['1fr', null, 'minmax(44ch, 50ch) 248px'],
      alignItems: 'start',
      gap: [3, null, 5],
    }}>
      <div sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
        <p sx={{ variant: 'text.heroKicker' }}>
          Software × Hardware × Fabrication
        </p>

        <h1 sx={{ variant: 'text.heroTitle' }}>
          Josh Wentworth
        </h1>

        <p sx={{ variant: 'text.heroSub' }}>
          Full-stack engineer with a workshop habit. I ship web apps (React/Angular + TypeScript, Node.js/Python) and the systems behind them (Kubernetes on GCP), and I also prototype electronics, lighting, and fabricated parts when software needs a body.
        </p>

        <div sx={{
          display: 'flex',
          alignItems: 'center',
          gap: 2,
          flexWrap: ['wrap', null, 'nowrap'],
          pt: 1,
        }}>
          <ScrollButton href="#projects" sx={{ variant: 'buttons.primary' }} offset={1.65}>
            See what I've built
          </ScrollButton>
          <ScrollButton href="#contact" sx={{ variant: 'buttons.secondary' }} offset={4}>
            Say hello
          </ScrollButton>
        </div>

        <p sx={{ variant: 'text.heroProof' }}>
          Most recently: cloud architecture and partner integrations for robotic grocery fulfillment at Fulfil Solutions.
        </p>

        <p sx={{ variant: 'text.micro' }}>
          React • Angular • TypeScript • Node.js • Python • Kubernetes • GCP • MySQL/Redis • Grafana/Loki/Elastic
        </p>
      </div>

      <div sx={{ pt: 1 }}>
        <div sx={{
          position: 'relative',
          width: ['176px', '208px', '232px'],
          height: ['176px', '208px', '232px'],
          borderRadius: 'lg',
          overflow: 'hidden',
          border: '1px solid',
          borderColor: 'border',
          boxShadow: 'softLg',
        }}>
          <GradientGlow />
          <img src="/avatar.jpg" alt="Josh Wentworth headshot" sx={{ width: '100%', height: '100%', objectFit: 'cover' }} />
        </div>
      </div>
    </div>
  </>
)
