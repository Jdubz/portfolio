# Cloud Functions Environment Variables
# Copy this file to .env for local development

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================

# Environment name (development, staging, production)
# Determines which Firestore database to use
ENVIRONMENT=development

# Node environment (development, test, production)
NODE_ENV=development

# ============================================================================
# FIRESTORE CONFIGURATION
# ============================================================================

# Firestore database ID (optional - will be auto-selected based on ENVIRONMENT)
# Options:
#   - (default) - for local emulator
#   - portfolio-staging - for staging environment
#   - portfolio - for production environment
# FIRESTORE_DATABASE_ID=

# Firestore emulator host (set automatically by Firebase emulators)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# ============================================================================
# FUNCTION CONFIGURATION
# ============================================================================

# Functions emulator flag (set automatically by Firebase emulators)
# FUNCTIONS_EMULATOR=true

# Log execution IDs for debugging
LOG_EXECUTION_ID=true

# ============================================================================
# NOTES
# ============================================================================
#
# Database Selection Logic:
# 1. If FIRESTORE_DATABASE_ID is set, use that value
# 2. If emulator is detected, use "(default)"
# 3. If ENVIRONMENT=staging, use "portfolio-staging"
# 4. If ENVIRONMENT=production, use "portfolio"
# 5. Otherwise, default to "portfolio" (production)
#
# For local development with emulators:
#   - Leave FIRESTORE_DATABASE_ID unset
#   - Emulators will automatically set FIRESTORE_EMULATOR_HOST
#   - Database will default to "(default)"
#
# For staging deployment:
#   - Set ENVIRONMENT=staging in Cloud Function configuration
#   - Database will be "portfolio-staging"
#
# For production deployment:
#   - Set ENVIRONMENT=production in Cloud Function configuration
#   - Database will be "portfolio"
