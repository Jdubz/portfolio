<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{name}} - Cover Letter</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica', 'Arial', sans-serif;
      font-size: 11pt;
      line-height: 1.6;
      color: #333;
      padding: 0;
      max-width: 8.5in;
    }

    .header {
      text-align: right;
      margin-bottom: 20px;
      padding-bottom: 12px;
      border-bottom: 1px solid #ddd;
    }

    .header .name {
      font-size: 16pt;
      font-weight: 600;
      color: #111;
      margin-bottom: 4px;
    }

    .header .email {
      font-size: 10pt;
      color: #666;
    }

    .header .email a {
      color: {{accentColor}};
      text-decoration: none;
    }

    .date {
      font-size: 10pt;
      color: #666;
      margin-bottom: 16px;
    }

    .greeting {
      font-size: 11pt;
      margin-bottom: 12px;
    }

    .content {
      margin-bottom: 12px;
    }

    .paragraph {
      margin-bottom: 12px;
      text-align: justify;
    }

    .closing {
      margin-top: 16px;
      margin-bottom: 24px;
    }

    .signature {
      font-size: 11pt;
    }

    .footer {
      position: fixed;
      bottom: 0.25in;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 8pt;
      color: #222;
      padding: 0 0.5in;
    }

    .footer a {
      color: {{accentColor}};
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    @media print {
      body {
        padding: 0;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <div class="name">{{name}}</div>
    <div class="email"><a href="mailto:{{email}}">{{email}}</a></div>
  </div>

  <!-- Date -->
  <div class="date">{{date}}</div>

  <!-- Greeting -->
  <div class="greeting">{{greeting}}</div>

  <!-- Content -->
  <div class="content">
    <div class="paragraph">{{openingParagraph}}</div>

    {{#each bodyParagraphs}}
      <div class="paragraph">{{this}}</div>
    {{/each}}

    <div class="paragraph">{{closingParagraph}}</div>
  </div>

  <!-- Closing & Signature -->
  <div class="closing">
    <div class="signature">{{signature}}</div>
  </div>

  <!-- Attribution Footer -->
  <div class="footer">
    Generated by a custom AI resume builder built by the candidate — <a href="https://joshwentworth.com/resume-builder" target="_blank">joshwentworth.com/resume-builder</a>
  </div>
</body>
</html>
