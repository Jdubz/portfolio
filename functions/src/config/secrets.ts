/**
 * Secret Configuration
 *
 * Defines secrets using Firebase Functions' params API.
 * Secrets are automatically loaded from Cloud Secret Manager.
 */

// No secrets currently in use for portfolio
// Previously contained AI API keys for resume generator (migrated to job-finder app)
