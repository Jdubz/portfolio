import ScrollButton from "../components/scroll-button"
import { GlowImage } from "../../../components/GlowImage"

export default () => (
  <>
    <div sx={{
      variant: 'layout.container',
      display: ['block', null, null, 'grid'],
      gridTemplateColumns: ['1fr', null, null, '2fr 1fr'],
      gap: [4, null, null, 5],
      alignItems: 'start',
    }}>
      <div sx={{ display: 'flex', flexDirection: 'column', position: 'relative' }}>
        {/* Backdrop blur layer behind all text */}
        <div sx={{ variant: 'masks.soft' }} />

        <span sx={{
          fontSize: [1, 1],
          fontWeight: 600,
          color: 'primary',
          letterSpacing: '0.08em',
          textTransform: 'uppercase',
          opacity: 0.9,
          mb: 3,
        }}>
          SOFTWARE × HARDWARE × FABRICATION
        </span>

        <h1 sx={{
          fontSize: ['48px', '52px', '56px'],
          fontWeight: 700,
          lineHeight: 1.12,
          color: 'heading',
          letterSpacing: '-0.01em',
          mb: 3,
        }}>
          Josh Wentworth
        </h1>

        <p sx={{ fontSize: [2, 3], lineHeight: 1.65, color: 'textMuted', maxWidth: '64ch', mb: 4 }}>
          Full-stack engineer with a workshop habit. I ship web apps (React/Angular + TypeScript, Node.js/Python) and the systems behind them (Kubernetes on GCP), and I also prototype electronics, lighting, and fabricated parts when software needs a body.
        </p>

        <div sx={{
          display: 'flex',
          gap: 3,
          mt: 4,
          mb: 4,
        }}>
          <ScrollButton offset={1.2}>
            See what I've built
          </ScrollButton>
          <ScrollButton offset={4} sx={{ variant: 'buttons.secondary' }}>
            Say hello
          </ScrollButton>
        </div>

        <p sx={{ fontSize: [2, 3], fontStyle: 'italic', color: 'text', borderLeft: '3px solid', borderColor: 'primary', pl: 3, py: 2, opacity: 0.95, mb: 3 }}>
          Most recently: cloud architecture and partner integrations for robotic grocery fulfillment at Fulfil Solutions.
        </p>

        <p sx={{
          fontSize: '14px',
          color: 'textMuted',
          opacity: 0.75,
          lineHeight: 1.5,
        }}>
          React • Angular • TypeScript • Node.js • Python • Kubernetes • GCP • MySQL/Redis • Grafana/Loki/Elastic
        </p>
      </div>

      <div sx={{
        display: 'flex',
        justifySelf: ['center', null, null, 'end'],
        alignSelf: 'start',
        mt: [4, null, null, 0],
      }}>
        <GlowImage
          src="/avatar.jpg"
          alt="Josh Wentworth headshot"
          width={[200, 240]}
        />
      </div>
    </div>

  </>
)
