#!/usr/bin/env sh

# Run linting (soft warning)
npm run lint || echo "⚠️  Lint warnings detected but allowing commit to proceed"

# Check if changeset might be needed (gentle reminder, not a blocker)
node scripts/check-changeset.js || true
